'use strict';

module.exports = function(mongoose) {
    //mongoose.model('Product').remove(function (error) {
    //    if (error) throw error;
    //
    //    mongoose.model('Product').create();
    //});

}
